name: Watcher docker release

on:
  release:
    types: [published]

jobs:
  get-version:
    # only run for releases with tag 'iexec-market-watcher-v*' as created by release-please for API
    if: startsWith(github.ref_name,'iexec-market-watcher-v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22"
      - name: Set publish version
        id: set-publish-version
        working-directory: watcher
        run: |
          VERSION=$(npm pkg get version | tr -d '"')
          echo "VERSION=${VERSION}" | tee -a $GITHUB_OUTPUT
    outputs:
      version: ${{ steps.set-publish-version.outputs.VERSION }}

  docker-publish:
    if: startsWith(github.ref_name,'iexec-market-watcher-v')
    needs: get-version
    uses: ./.github/workflows/reusable-watcher-docker.yml
    with:
      tag: ${{ needs.get-version.outputs.version }}
    secrets:
      docker-username: ${{ secrets.DOCKERHUB_USERNAME }}
      docker-password: ${{ secrets.DOCKERHUB_PAT }}
      dockerhub-username: ${{ secrets.DOCKERHUB_USERNAME }}
      dockerhub-password: ${{ secrets.DOCKERHUB_PAT }}
